# Portfolio Website

A simple personal portfolio website for Sachithanantham P.

## Run locally

Open index.html in a browser, or serve the folder with a local server:

```bash
python -m http.server 8000
```

Then visit http://localhost:8000
